int creatureId = -1;

create()
{
    name = "apple";
    health = 100;
    energyPerTurn = 4;
    skillSet = new Array();
    skillSet.add("move");
    skillSet.add("push");
    creatureId = game.createCreature(name, health, energyPerTurn, skillSet);
    return creatureId;
}