    import com.wartricks.components.ActionSequence;
    import com.wartricks.components.Action;
    import com.wartricks.custom.Pair;
    
    creature = game.world.getEntity(caster);
    sequence = game.world.getMapper(ActionSequence.class).get(creature);
    sequence.onBeginTurnActions.add(new Action(caster, 0, origin, new Pair(origin.x, origin.y - 1)));
    creature.changedInWorld();